# Configuration

This directory is reserved for configuration that should be kept under version control as the physical GLaDOS hardware is developed.

Planned configuration includes:

- GPIO assignments
- Display settings
- NeoPixel settings
- Servo channels and limits
- Animation timing
- Hardware-specific options

Do not commit secrets, credentials, or machine-specific private configuration. Local overrides should remain outside version control.
