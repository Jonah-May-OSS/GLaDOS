name: Python quality

on:
  push:
    branches: [main, "ci/**"]
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.13"

      - name: Ruff lint
        run: uv run --with ruff --with pillow --with numpy --with websockets ruff check .

      - name: Ruff format
        run: uv run --with ruff ruff format --check .

      - name: ty
        run: uv run --with ty --with pillow --with numpy --with websockets ty check

      - name: Compile Python
        run: uv run python -m compileall -q display
